# =========================
# 💡 API GATEWAY CONFIGURATION
# =========================

server.port=8080
spring.application.name=api-gateway

# =========================
# 🔎 EUREKA CONFIGURATION (Discovery Client)
# =========================
# URL donde está corriendo el servidor de Eureka (usa el nombre del servicio Docker)
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
# Registrar el servicio en Eureka
eureka.client.register-with-eureka=true
# Obtener información de otros servicios desde Eureka
eureka.client.fetch-registry=true

# =========================
# 🔒 SECURITY CONFIGURATION (Keycloak)
# =========================
# ✅ CORREGIDO: Adaptarse a lo que Keycloak emite (keycloak:8080)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://keycloak:8080/realms/tpi-realm
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://keycloak:8080/realms/tpi-realm/protocol/openid-connect/certs
spring.security.oauth2.resourceserver.jwt.connect-timeout=15000
spring.security.oauth2.resourceserver.jwt.read-timeout=15000

# =========================
# 🔄 GATEWAY ROUTES CONFIGURATION (MOVIDO A GatewayRouteConfig.java)
# =========================

# NOTA: Las rutas deben usar ahora el load balancer de Eureka (ej: lb://MS-CLIENTES)

# -------------------------
# 📝 CONFIGURACIÓN DE LOGGING (DEBE ESTAR EN DEBUG)
# -------------------------

# Habilitar logs detallados de Spring Security (CRÍTICO PARA DEBUGGING 401/403)
logging.level.org.springframework.security=DEBUG 

# Habilitar logs detallados de Spring Cloud Gateway 
logging.level.org.springframework.cloud.gateway=DEBUG 

# Habilitar logs detallados para Eureka Client
logging.level.com.netflix.discovery=DEBUG
logging.level.org.springframework.cloud.netflix.eureka=DEBUG

# Para logs más detallados de errores de red
logging.level.reactor.netty.http.client=DEBUG

# Opcional: Configurar el patrón de log para incluir el nombre de la aplicación
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{39}): %msg%n

# -------------------------
# 📊 CONFIGURACIÓN DE MÉTRICAS (ACTUATOR)
# -------------------------
# Para habilitar las métricas de Gateway
spring.cloud.gateway.metrics.enabled=true 

# Habilitar tags de ruta en las métricas (opcional)
spring.cloud.gateway.metrics.tags.path.enabled=true

# 💡 AÑADIDO: Exponer los endpoints de Actuator para que puedas consultarlos y monitorear el Gateway
management.endpoints.web.exposure.include=metrics,health,gateway

# =========================
# 🚫 SECURITY DISABLED (ELIMINADO para usar SecurityConfig.java)
# =========================